# Converter Tools

Utilities for converting mutation and SNP formats used across external databases.

- `convert_mutation_style.py`: convert three-letter amino acid notation to one-letter style
- `convert_snps_to_amino_acid.py`: convert nucleotide SNP edits to amino-acid substitutions

These scripts call reusable converter functions in `src/gam/converters/`.
